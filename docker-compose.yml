name: zhituo-ai

services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      APP_ENV: ${APP_ENV:-local}
      FRONTEND_DIR: /app/frontend
      DEBUG: ${DEBUG:-true}
      DATABASE_URL: postgresql+psycopg://zhituo:${POSTGRES_PASSWORD:-zhituo_dev}@postgres:5432/${POSTGRES_DB:-zhituo}
      REDIS_URL: redis://redis:6379/0
      CORS_ORIGINS: ${CORS_ORIGINS:-http://127.0.0.1:8766,http://localhost:8766}
      TRUSTED_HOSTS: ${TRUSTED_HOSTS:-127.0.0.1,localhost,api}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-local-development-only-change-me}
      REFRESH_TOKEN_EXPIRE_DAYS: ${REFRESH_TOKEN_EXPIRE_DAYS:-7}
      REFRESH_COOKIE_SECURE: ${REFRESH_COOKIE_SECURE:-false}
      BOOTSTRAP_ADMIN_USERNAME: ${BOOTSTRAP_ADMIN_USERNAME:-groupadmin}
      BOOTSTRAP_ADMIN_EMPLOYEE_NO: ${BOOTSTRAP_ADMIN_EMPLOYEE_NO:-CMCC0001}
      BOOTSTRAP_ADMIN_DISPLAY_NAME: ${BOOTSTRAP_ADMIN_DISPLAY_NAME:-集团管理员}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD:-szyd123456}
      BOOTSTRAP_DEMO_USERS: ${BOOTSTRAP_DEMO_USERS:-true}
      BOOTSTRAP_DEMO_USER_PASSWORD: ${BOOTSTRAP_DEMO_USER_PASSWORD:-szyd123456}
      QCC_APP_KEY: ${QCC_APP_KEY:-}
      QCC_SECRET_KEY: ${QCC_SECRET_KEY:-}
      TENCENT_MAP_KEY: ${TENCENT_MAP_KEY:-}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      QWEN_API_KEY: ${QWEN_API_KEY:-}
      GLM_API_KEY: ${GLM_API_KEY:-}
      KIMI_API_KEY: ${KIMI_API_KEY:-}
      CLAMAV_HOST: ${CLAMAV_HOST:-}
      CLAMAV_PORT: ${CLAMAV_PORT:-3310}
    ports:
      - "${API_BIND_ADDRESS:-127.0.0.1}:8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: >-
      sh -c "alembic upgrade head &&
      python -m app.scripts.seed &&
      uvicorn app.main:app --host 0.0.0.0 --port 8000"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health/live')"
      interval: 10s
      timeout: 3s
      retries: 8

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-zhituo}
      POSTGRES_USER: zhituo
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-zhituo_dev}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zhituo -d ${POSTGRES_DB:-zhituo}"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres_data:
  redis_data:
